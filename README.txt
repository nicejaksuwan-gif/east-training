EAST Training - Lock after 3 failed attempts
Default Admin PIN: EAST2026
Change ADMIN_PIN in index.html before publishing.
Important: on GitHub Pages, lock state is stored only in the same browser/device via localStorage. For central admin unlock across devices, use a backend.
